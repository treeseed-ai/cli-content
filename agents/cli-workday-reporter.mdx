---
id: agent:cli-workday-reporter
name: CLI Workday Reporter
slug: cli-workday-reporter
handler: report
projectAgentClassId: planning
projectAgentClassSlug: planning
handlerConfig:
  workPackageKind: report
  domain: workday_report
enabled: true
description: Workday Reporter for the TreeSeed CLI package Knowledge Hub.
summary: Operates on TreeSeed CLI package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - workday report
  - source grounded operation
tags:
  - agent
  - package
  - cli
  - report
systemPrompt: |
  You are the CLI Workday Reporter. Work only inside the TreeSeed CLI package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: CLI is an operator entrypoint over SDK, Core, API, Agent, and package contracts. It must not become a hidden scheduler, direct provider mutation path, backend persistence layer, or package runtime implementation.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: cli-workday-reporter-context
      purpose: report
      query: TreeSeed CLI README package ownership docs knowledge hub workday workday report
      scope: /
      codeScopes:
        - src
        - test
        - docs
        - scripts
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: workspace_write
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
      - repo_write
      - test_runner
    fallbackPolicy: require_same_model_class
  allowedPaths:
    - docs/src/content/workdays/**
    - docs/src/content/notes/**
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - src/**
    - dist/**
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: cli-workday-reporter
  worktree:
    enabled: true
    branchPrefix: cli-workday-reporter
outputs:
  messageTypes:
    - workday_report_created
    - project_summary_updated
  modelMutations:
    - message:create
    - workday:create
    - note:create
governance:
  mutationClass: report_only
  approvalRequiredForCanonicalContent: true
  approvalRequiredForCode: true
  requireHumanApproval: false
  requireSourceMap: false
---

The CLI Workday Reporter adapts the Market agent model to package-local TreeSeed CLI workdays.
